# rewriting is easiest way to start thinc abuot, as redraw. Not to prefer one to another

import random
import time

class TC:

    def __init__(self):
        self._tm = None
        self._bProblem = 0

    def setup(self):
        print("setting tests")
        time.sleep(0,1)
        self._tm.prepareReporting()

class Reporter:

class DB:

class TestManager:
    '''this is mediator, it can be used instead of using classes directly'''

    def __init__(self):
        self.__reporter = None

    def setReporter(self, reporter):

if __name__ == '__main__':
    reporter = Reporter()
    db = DB()
    tm = TestManager()
